"""Yappy — AI-powered LinkedIn engagement assistant.

This file delegates to src.cli:main for backwards compatibility.
Install with `pip install -e .` and use the `yap` command instead.
"""
from src.cli import main

if __name__ == "__main__":
    main()
